<?xml version="1.0" encoding="utf-8"?>
<manifest package="tvao.mmad.itu.tingle"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.Activities.TingleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.Activities.ThingPagerActivity"/>
        <!--<activity android:label="SettingsActivity" android:name=".SettingsActivity">-->
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MANAGE_NETWORK_USAGE" />-->
                <!--<category android:name="android.intent.category.DEFAULT" />-->
            <!--</intent-filter>-->
        <!--</activity>-->
        <!--<activity android:name=".Controller.Activities.ThingListActivity"/>-->
    </application>

    <!-- Permissions prompted to user when downloading app-->

    <!-- Camera is used to register bar code-->
    <!-- Internet and access is used for network operations -->
    <!-- External storage is used for photos -->
    <!-- Fine and course location is used for location functionality using GPS or WIFI respectively  -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="18"/>
    <!--<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->
    <!--<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />-->


    <!-- Features that app relies on, e.g. camera -->
    <uses-feature android:name="android.hardware.camera"
                  android:required="false" />

    <!-- App may use GPS og Network to fetch user current location -->
    <uses-feature android:name="android.hardware.location"
                  android:required="false"/>


</manifest>
